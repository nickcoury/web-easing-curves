{
  "name": "web-easing-curves",
  "version": "1.0.0",
  "description": "A web app for visualizing easing curves",
  "main": "dist/main.js",
  "type": "module",
  "scripts": {\n    "build": "tsc && mkdir -p dist/assets && cp -r src/assets dist/assets 2>/dev/null || true && cp -f src/index.html dist/index.html || true",
    "dev": "concurrently \"tsc --watch\" \"live-server src\"",
    "preview": "live-server dist",
    "clean": "rm -rf dist"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^7.6.0",
    "live-server": "^1.2.0",
    "typescript": "^4.9.5"
  }
}
