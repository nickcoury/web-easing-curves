{
  "name": "web-easing-curves",
  "version": "1.0.0",
  "description": "A web app for visualizing easing curves",
  "main": "docs/main.js",
  "type": "module",
  "scripts": {
    "build": "tsc && mkdir -p docs/assets && cp -r src/assets docs/assets 2>/dev/null || true && cp -f src/index.html docs/index.html || true",
    "dev": "concurrently \"tsc --watch\" \"live-server src\"",
    "preview": "live-server docs",
    "clean": "rm -rf docs",
    "smoke": "node -e \"require('fs').statSync('docs/main.js'); console.log('OK');\"",
    "dev:static": "npx live-server docs --port=5173",
    "test-build": "npm run build && npm run smoke"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^7.6.0",
    "live-server": "^1.2.0",
    "typescript": "^4.9.5"
  }
}
